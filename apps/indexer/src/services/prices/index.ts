import type { Token } from "../../database/schemas/public/Token";

export function prices(tokens: Token[]) {
	// TODO: get all reserves that have these tokens, or base tokens (usdt, alph)
	// TODO: return all tokens prices with source & liquidity
	// ayin-usdt
	// ayin-alph
	// average price
}
