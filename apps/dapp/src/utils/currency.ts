// import { Currency, useCurrency } from "../hooks/useCurrency";

// export function useFormatCurrency() {
// 	const { currency } = useCurrency();
// 	return {
// 		formatCurrency: (n: number, cur = currency.value) => formatCurrency(n, cur),
// 	};
// }
